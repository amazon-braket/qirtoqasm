# Full per-platform wheel + sdist matrix. Runs via `workflow_call` from
# `publish-to-pypi.yml` on release publication, or via manual
# `workflow_dispatch` for one-off testing. The PR-time smoke build lives
# in `wheel-smoke.yml`.
#
# Matrix: Linux (manylinux2014 + musllinux_1_2 on x86_64; manylinux2014
# on aarch64 via QEMU), macOS (x86_64 + arm64), Windows (x86_64). Because
# the extension is built with PyO3's ``abi3-py311`` feature, one wheel
# per platform covers Python 3.11, 3.12, and 3.13.
#
# Outputs are uploaded as artifacts. On release publication these are
# consumed by the `publish` job in `publish-to-pypi.yml`.

name: Build wheels

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  # Full matrix.
  build-wheels:
    name: Wheels ${{ matrix.os }} (${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest,  arch: x86_64,  cibw_arch: x86_64 }
          - { os: ubuntu-latest,  arch: aarch64, cibw_arch: aarch64 }
          - { os: macos-latest,   arch: x86_64,  cibw_arch: x86_64 }
          - { os: macos-latest,   arch: arm64,   cibw_arch: arm64 }
          - { os: windows-latest, arch: x86_64,  cibw_arch: AMD64 }

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      # QEMU required to cross-build aarch64 Linux wheels on an x86_64 runner.
      - name: Set up QEMU (for aarch64 Linux)
        if: matrix.os == 'ubuntu-latest' && matrix.arch == 'aarch64'
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64

      - name: Build wheels
        uses: pypa/cibuildwheel@v4.1.0
        env:
          CIBW_ARCHS: ${{ matrix.cibw_arch }}
          # ``cp311-*`` alone covers 3.11 / 3.12 / 3.13 via abi3.
          CIBW_BUILD: "cp311-*"
          CIBW_SKIP: "pp* *-musllinux_i686 *-win32 *-win_arm64"
          # Linux builds run inside manylinux containers which don't
          # ship Rust; install it before each job runs.
          CIBW_BEFORE_ALL_LINUX: "curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile minimal"
          CIBW_ENVIRONMENT_LINUX: 'PATH="$HOME/.cargo/bin:$PATH"'
          CIBW_BEFORE_ALL_MACOS: "rustup target add x86_64-apple-darwin"
          CIBW_ENVIRONMENT_MACOS: MACOSX_DEPLOYMENT_TARGET=${{ matrix.arch == 'x86_64' && '10.13' || '11.0' }}
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_MANYLINUX_AARCH64_IMAGE: manylinux2014
          CIBW_MUSLLINUX_X86_64_IMAGE: musllinux_1_2
          CIBW_TEST_COMMAND: 'python -c "import qirtoqasm; print(qirtoqasm.__version__)"'

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}-${{ matrix.arch }}
          path: ./wheelhouse/*.whl

  # Source distribution.
  build-sdist:
    name: sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.x"
      - uses: dtolnay/rust-toolchain@stable
      - name: Install maturin
        run: pip install 'maturin>=1.5,<2.0'
      - name: Build sdist
        run: maturin sdist --out dist
      - name: Upload sdist artifact
        uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: ./dist/*.tar.gz
